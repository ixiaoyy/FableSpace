export function buildAppPanelProps({
  advancedOpen,
  adminOpen,
  apiBase,
  autoEntering,
  checking,
  checkBackend,
  errorText,
  familiarityMap,
  feedback,
  form,
  ghostTraces,
  handleOrchestrationEvent,
  handlePoiClick,
  honorBoard,
  lastSessionAt,
  lastWritebackPoiId,
  locating,
  mapLayerPanelOpen,
  orchestrationEvents,
  originHint,
  originLabel,
  placeLegend,
  playerState,
  poiFactionFilter,
  poiFactionOptions,
  poiOnlyFamiliar,
  poiSearch,
  poiSearchSummary,
  poiTypeFilter,
  poiTypeOptions,
  setPoiFactionFilter,
  setPoiOnlyFamiliar,
  setPoiSearch,
  setPoiTypeFilter,
  presetMeta,
  recentEchoes,
  recentMarks,
  resolvedActivePoi,
  result,
  setAdminOpen,
  setAdvancedOpen,
  setApiBase,
  setAllMapLayers,
  setMapLayerPanelOpen,
  statusDetail,
  statusOk,
  statusText,
  submitNearby,
  submitWriteback,
  submitting,
  updateForm,
  updateWritebackForm,
  useCurrentLocation,
  usePreset,
  visibleMapLayers,
  sliceAtmosphere,
  behaviorInsights,
  writebackError,
  writebackForm,
  writebackResidues,
  writebackResult,
  writebackSubmitting,
  writebackTargetSummary,
  writebackTimeline,
  revisitSummary,
  selectedActionMeta,
  selectedVisibilityMeta,
  filteredWorldPois,
  disturbanceForm,
  setDisturbanceForm,
  disturbanceSubmitting,
  submitDisturbance,
  disturbanceActive,
  clearDisturbance,
  visibilityOptions,
  focusWritebackTarget,
  sliceHighlights,
  activePoiId,
  applyMapLayerPreset,
  applyWritebackAction,
  resetMapLayers,
  toggleMapLayer,
  locationPresets,
  mapLayerOptions,
  mapLayerPresets,
  writebackActions,
}) {
  return {
    entryPanelProps: {
      lastSessionAt,
      originLabel,
      originHint,
      form,
      locationPresets,
      presetMeta,
      usePreset,
      locating,
      autoEntering,
      useCurrentLocation,
      submitting,
      submitNearby,
      advancedOpen,
      setAdvancedOpen,
      updateForm,
      errorText,
      result,
    },
    resultPanelProps: {
      result,
      statusOk,
      sliceAtmosphere,
      sliceHighlights,
    },
    mapStageProps: {
      result,
      originLabel,
      form,
      mapLayerPanelOpen,
      setMapLayerPanelOpen,
      visibleMapLayers,
      mapLayerOptions,
      mapLayerPresets,
      applyMapLayerPreset,
      setAllMapLayers,
      resetMapLayers,
      toggleMapLayer,
      activePoiId,
      familiarityMap,
      ghostTraces,
      handlePoiClick,
      writebackForm,
      handleOrchestrationEvent,
      submitting,
      submitNearby,
      resolvedActivePoi,
      writebackResult,
      selectedVisibilityMeta,
      poiSearch,
      setPoiSearch,
      poiTypeFilter,
      setPoiTypeFilter,
      poiTypeOptions,
      poiFactionFilter,
      setPoiFactionFilter,
      poiFactionOptions,
      poiOnlyFamiliar,
      setPoiOnlyFamiliar,
      poiSearchSummary,
      filteredWorldPois,
      orchestrationEvents,
      disturbanceForm,
      setDisturbanceForm,
      disturbanceSubmitting,
      submitDisturbance,
      disturbanceActive,
      clearDisturbance,
      selectedActionMeta,
      visibilityOptions,
      writebackTargetSummary,
      writebackActions,
      applyWritebackAction,
      updateWritebackForm,
      writebackSubmitting,
      submitWriteback,
      writebackError,
      writebackTimeline,
      revisitSummary,
      writebackResidues,
      behaviorInsights,
      focusWritebackTarget,
      lastWritebackPoiId,
    },
    adminPanelProps: {
      adminOpen,
      setAdminOpen,
      apiBase,
      setApiBase,
      checking,
      checkBackend,
      statusOk,
      statusText,
      statusDetail,
      writebackForm,
      updateWritebackForm,
      writebackSubmitting,
      submitWriteback,
      writebackError,
      writebackResult,
      playerState,
      feedback,
      recentEchoes,
      recentMarks,
      placeLegend,
      honorBoard,
    },
  }
}
