# Verification — 安妮·伦敦宽街完整故事

日期：2026-07-22

## 交付证据

- 剧情图：15 个节点、28 个选择、5 个私人完成结局；所有 `next_node_id` 闭合，带 `completes` 的选择均指向完成节点。
- 固定历史：五个完成节点都保留“9 月 7 日陈述、次日移除泵柄、暴发此前已经开始减退、玩家纸页不决定公共历史”。
- 聊天边界：结构化历史选择携带精确上下文标记，只在显式 allow-list 的历史 Space 中走人工审阅的确定性回复；自由输入仍走常规角色聊天。
- 前端体验：进入安妮后自动开局；选择先写入私聊再推进 Gameplay；隐藏通用任务/模板/礼物等干扰；史料抽屉按开场、调查、结局逐步解锁。

## 自动验证

| 命令 | 结果 |
|---|---|
| `py -3 -m compileall -q apps/api/src` | PASS |
| 后端定向剧情图与确定性回复脚本 | PASS：`nodes=15 complete=5 choices=28 trust_rule=specific` |
| `npm --prefix .\apps\web run typecheck` | PASS |
| `npm --prefix .\apps\web run build` | PASS，React Router/Vite 生产构建成功 |
| `git diff --check` | PASS；仅输出工作区既有的 LF/CRLF 提示，无空白错误 |

React Doctor：

- 全仓扫描在既有 `app/product/SpaceChatRoom.jsx` 上触发 `oxlint-plugin-react-doctor` 栈溢出，未形成可用全仓分数；同时只报告该旧文件一条动态 HTML 注入警告。
- 变更文件扫描成功，14 条均落在既有大型 `SpaceChatWorkbench` 结构；按变更行扫描剩 2 条：一条是既有多 `useState` 架构建议，另一条把明确位于 `finally` 内的 `setIsGameplayBusy(false)` 误报为不在 `finally`。本轮新增的专用历史故事组件无告警。

## 390×844 浏览器验收

Verdict：**PASS**

- 自动出现安妮开场、三条入口选择和自由输入框；没有通用任务进度、玩法说明或开口模板。
- 实际走通 `询问水泵 → 找拿名单的医生 → 只写确实知道的取水处 → 让安妮自己说`。
- 每个选择都先出现在私聊，随后场景推进；结构化回复没有再生成具名住户、精确日期、伤亡数字或水泵外观。
- 结局只改变安妮对访客的私人信任；文本明确公共调查早已开始，泵柄移除和疫情走势不由玩家决定。
- 史料条目从开场 2 条、调查 4 条解锁到结局 7 条；包含精确提示 `含 AI 总结；剧情设定可能包含虚构。`。
- 史料抽屉与页面均无横向溢出，输入区保持可见；刷新后重新开局，输入框可填写和清空。

## 对抗式史实自审

Verdict：**PASS**

- 安妮、家庭、对话和关系结果均标记为 `剧情设定`。
- 没有把点图写成 Snow 调查起点，没有把移除泵柄写成即时终结疫情，也没有让玩家促成公共结果。
- 结局史料明确 Snow 在泵柄移除前已观察到暴发减退。
- 运行时实测发现提示词不足以阻止模型补写未核验细节后，已把结构化选择收紧为人工审阅的确定性回复，并写入代码规范。

## 剩余边界

- 浏览器人工验收覆盖一个完整信任路径；其余四个结局由闭合图校验和固定结局文本校验覆盖，未逐条人工点击。
- 自由输入仍使用配置的 AI 聊天后端，因此继续依赖角色系统提示和现有降级规则；只有已发布的结构化选择保证确定性回复。
- 本轮没有生成或引用新的图片资产，`.codex/generated_images` 无需上传或登记。
