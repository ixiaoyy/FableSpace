# 先天二年·虔化门本地试玩记录

核验日期：2026-08-05

## 测试边界

- 未连接数据库。浏览器使用只存在于本机内存中的临时 API 桩，Character 详情和剧情节点直接读取当前 `STORY_WORLD_REGISTRY`；结束后已停止服务并删除诊断代码。
- 新图从仓库忽略的 WebP 上传候选目录加载，未把图片二进制写入 Git。
- 后端另做图遍历：每个 PlayerRole 有 24 条可达选择路径，两个身份合计模拟 48 条，四个结局各自每身份可由 6 条路径到达。

## 浏览器路线 A

- 入口：太平公主 → 宫人。
- 自由输入：询问“真正的秘密”和是否亲见谋反。
- 结果：Character 回应保持固定“剧情转述（非史料原话）”前缀，只确认公开行动并说明其余无法核验。
- 选择：追查“同谋 / 谋废”来源 → 将无法核验的逐句密谋留白 → 并列冲突说法。
- 结局：`两说并存`；页面明确显示太平公主一方失败、公主随后被赐死、玄宗取得更完整的最高权力。
- 史料面板：6 条史实、4 条剧情设定、0 条待核验；官修史书定性条目明确说明只确认来源立场，不确认指控等同完整内心真相。

## 浏览器路线 B

- 入口：高力士 → 内侍小使。
- 选择：区分普通宫人与被指控者 → 普通当值者另列 → 只带走普通宫人的交接名单。
- 结局：`无名者的名单`；只改变原创普通人物的记录处境，固定历史结果与路线 A 完全一致。

## 视觉与交互

- 桌面：首页、两个 Character 卡、太平公主详情、双 PlayerRole 卡、故事时间线、史料面板和两个结局均可见且可操作。
- 390×844 移动端：首页与故事页 `document.scrollWidth <= window.innerWidth`，无横向溢出；四张候选图均正常加载。
- 浏览器日志：`error / warn / warning` 为 0。
- 发现并修复：Character 详情页原先连续显示两次“先天二年·虔化门”；路由场景标签改为“唐长安宫城 · 713”，世界标题保留。

## 生产验收

- 管理后台 `/admin/story-worlds` 显示“先天二年·虔化门”，旧宫廷标题不再出现。
- 公开首页显示安妮、高力士、太平公主三名 Character；两张新 Character 图均完整加载，高力士与太平公主卡片展示对应史料边界摘要。
- 高力士详情页显示“唐长安宫城 · 713”、固定“剧情转述（非史料原话）”文案，以及“内侍小使 / 宫人”两个成年 PlayerRole；Character 与两个 PlayerRole 图片均完整加载。
- 公开 API 独立核验内容版本为 `palace-xiantian-coup-2026-08-05.1`，两名 Character、两个 PlayerRole 与四个新 URL 均匹配注册表。
- 四个旧 URL 删除后均返回 `404`，四个新 URL 仍返回 `200 image/webp`；线上未见旧标题、旧姓名或旧图片引用。

## Verdict

`PASS`

代码、内容、两身份关键路线、桌面 / 移动端表现、生产公开入口与媒体加载均通过验证；托管内容和旧对象清退结果与本地审核版本一致。
