# S2-C 树枝交互与采集经验

日期：2026-09-10；本地 `main`，起点 `173bebc5`。根据本任务已记录的连续开发授权推进一个独立批次。

## 实际变化与依据

现有 `fallen-branch` 改为使用斧头砍断，获得木材1和采集经验1。斧头耗能复用当前熟练度规则，升级发生在本次扣费之后；空手交互提示使用斧头。普通树木、野花和春笋不改动。

依据为本日读取的 [The Farm / Wood](https://stardewvalleywiki.com/The_Farm#Wood) 与 [Foraging / Experience Points](https://stardewvalleywiki.com/Foraging#Experience_Points)。页面支持工具、产物和经验，不代表已经运行原作1.6.15核验。原有每日/风天刷新、点击距离、直接入包和满包整次拒绝继续保留为待对齐内容，未将其设为永久例外。

## 实际验证

- `npm --prefix .\apps\mirror-island run typecheck:client` 通过，包含内容类型、Godot导入/解析和无窗口启动。
- `artifacts/branches-2026-09-10/validate_branches.gd` 使用内存仓库执行15项检查全部通过：新建、四种错误手持物品、体力不足、满包、场景目标选取、保存失败不发布、重复重试一次清除/木材/经验、99→100经验即时升级、动作前等级扣费、重复命中不变、同版本继续。
- 首轮恢复比较失败来自原始浮点数与JSON解码规范化整数的深层比较；继续入口和解码均成功。诊断确认逐字段值一致后，检查两侧使用项目既有 `normalize_numbers`，无存档生产代码修改。
- `git diff --check` 通过。未扩建测试矩阵，诊断脚本留在忽略目录，不加入游戏入口或Git。

## 存档、限制与回退

封套8/状态20、记录ID和状态结构不变，同版本记录可继续；已有当日已采集枝条保持已清除，不追溯发放经验。错误动作和保存失败不提前发布，重试写入同一候选。

未进行真实键盘/触屏完整动作验收、实际IndexedDB/Windows存档恢复、图形手感或两端重新导出。无需新配置、依赖或migration，没有数据库连接、素材上传、提交、推送或部署。四个生产文件已暂存，文档与诊断产物未自动暂存。

回退须按本批diff一起恢复资源判定、场景选取、成功码及文字/声音/木屑映射；无schema回滚。已获得木材和经验属于普通状态，回退代码不会自动撤回，不修改玩家存档。后续继续补齐野采身份、钓鱼、配方和职业等任务剩余项。
