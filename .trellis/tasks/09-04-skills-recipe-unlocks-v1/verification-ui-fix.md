# UI 修复与发布前验证

2026-09-11：按用户要求修复上一轮 UI 验收问题，并提交推送 main 后继续生产验收。

- 删除启动页无条件的旧开发档提示，真实存档读取失败仍显示已有错误消息。
- 菜单打开后聚焦首个可用按钮；没有可操作内容时使用返回按钮。保存失败继续由重试框持有焦点。
- 普通按钮显式启用键盘焦点，Button、OptionButton、LineEdit 共用深绿焦点边框。
- 本地隐藏浏览器实际验证：继续验收档、Escape 打开菜单、首项焦点可见、Tab 到制作、Return 打开制作。
- 钓鱼检查 73/73、场景检查 33 项、类型/导入和 Web 导出通过。
- 发布前纠正大头鱼对德米特里厄斯和威利的偏好为 neutral；普通送礼 +20，保持此前其它物品偏好。

Godot 官方 Web demo 页面明确说明屏幕阅读器支持尚不适用于 Web 平台：https://godotengine.github.io/godot-demo-projects/ 。本次未添加网页语义桥接，因此辅助功能树缺口仍未关闭，不能标为完整无障碍验收通过。

提交包含此前围栏、河流鱼池、鱼获品质和大头鱼批次及配套记录。没有新增数据库迁移。回滚需使用完整前端版本；此前封套 13 / 状态 25 的存档不能交给更早 schema 的客户端解释。
