from __future__ import annotations

from typing import Any


def normalize_skill_pack_settings(_value: Any) -> list[dict[str, Any]]:
    """Retire legacy user-configurable skill packs during Space migration."""

    return []
