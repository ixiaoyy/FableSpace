# 小镇住宅与草地底纹精修

日期：2026-09-10。用户要求继续处理上一轮小镇重复屋型与草地纹理问题。

## 修改

- 保留西侧橙顶木屋。中部住宅使用现有浅色立面、橙顶；东南住宅使用蓝顶、浅色立面。仅替换 town.tmj 的 Buildings / AbovePlayer 共 40 个瓦片，包含屋顶首行留空；原来的门口、碰撞、出口、交互和 NPC 位置不变。
- 所有建筑图像复用已登记 VectoRaith buildings 原图，没有编辑原 PNG、新增远端对象或引入图片二进制。蓝顶组合检查时发现顶排最右格属于相邻草堆，最终已将该格清空。
- 农场与小镇的 Ground 层均经检查只含 GID 18 草地。为该层应用 pastoral_grass.gdshader，将原色向春绿基色混合 45%，降低细小明暗噪点；不使用模糊过滤，保留 alpha，不处理 GroundDetail 的道路、水面、树木、人物或建筑。
- 着色器为本轮 Codex 编写的运行时表现代码，不是生成图片；无需图像服务 prompt 或新增 CDN 元数据。未更换像素尺寸或相机缩放。

## 实际验证

- check-map.mjs 对比本轮前暂存版本：小镇只改 40 个视觉瓦片，其余全部图层与对象内容一致；农场和小镇 Ground 均为纯草地。
- 最终 typecheck:client、build:client、build:windows 通过，无 SCRIPT ERROR / ERROR。
- 普通 Web 构建在审查来源旧农场继续，实景检查农场草地、道路和桥面，走到小镇核对最终住宅拼接。截图：artifacts/town-art-fix-2026-09-10/01-farm.png、02-town-final.png。没有以先前截图代替本轮复验。
- 未新增玩法测试、依赖、保存字段、数据库连接或远端写入。代码与地图暂存，文档和截图不自动暂存；未提交或部署。

## 剩余与回退

本次区分了立面及屋顶组合，未声称完整原创建筑造型已完成。树形重复、居民与主角的比例差异、完整天气 / 夜间观感和真机性能仍待验收。草地 shader 仅适用于当前纯草地 Ground；将来该层混入其他地貌时须重新限定处理范围。

回退只撤回本轮 40 个瓦片、_load_region 中的草地材质应用和 pastoral_grass.gdshader；不回退此前桥板、农舍、工具、角色预览及首屏 UI，不涉及存档转换。
